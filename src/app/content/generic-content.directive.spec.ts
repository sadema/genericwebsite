import { GenericContentDirective } from './generic-content.directive';

describe('GenericContentDirective', () => {
  it('should create an instance', () => {
    const directive = new GenericContentDirective();
    expect(directive).toBeTruthy();
  });
});
