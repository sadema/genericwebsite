import { GenericContainerDirective } from './generic-container.directive';

describe('GenericContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new GenericContainerDirective();
    expect(directive).toBeTruthy();
  });
});
